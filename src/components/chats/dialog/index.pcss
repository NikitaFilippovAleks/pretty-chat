.chats-dialog {
  display: flex;

  flex: 1;
  flex-direction: column;

  width: 100%;
  height: 100%;

  &__messages {
    display: flex;

    flex: 1;
    flex-direction: column;

    justify-content: flex-end;

    padding: 20px;
  }
}
