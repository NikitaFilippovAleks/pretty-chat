{{#>form}}
  <div class="formProfileEditData__info">
    {{#if isLoading}}
      <div class="formProfileEditData__info__throbber">
        {{> throbber}}
      </div>
    {{else}}
      {{#each inputs}}
        {{{this}}}
      {{/each}}
    {{/if}}
  </div>

  <div class="formProfileEditData__buttons">
    {{> buttonMain text='Save' class='primary' type='submit'}}
  </div>
{{/form}}
