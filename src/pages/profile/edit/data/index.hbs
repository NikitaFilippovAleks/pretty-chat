{{#> layoutMain }}
  {{{navigation}}}

  {{#> layoutWindow }}
    <div class="layout-profile__back-button">
      {{> buttonBack text='Profile' modifier='light' light=true}}
    </div>
  
    <div class="layout-profile">
      <div class="profile-edit-data__avatar">
        {{> profileAvatar text=false}}
      </div>

      {{{form}}}
    </div>
  {{/layoutWindow}}
{{/layoutMain}}
